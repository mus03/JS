// Task-1
// Take four parameters. Multiply the four numbers and then return the result

function multiply(a, b, c, d) {
  const value = a * b * c * d;
  return value;
}

const value = multiply(1, 2, 3, 4);
console.log("Multiply the four numbers: ", value);
